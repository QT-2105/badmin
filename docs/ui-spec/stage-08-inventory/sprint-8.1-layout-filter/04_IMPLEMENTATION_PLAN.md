# Implementation Plan

1. Preserve `reportPeriod`, `reportMonth`, `reportYear`, and handlers. Done.
2. Replace the report-period `ToolbarCard` presentation with shared `FilterBar`. Done.
3. Refine page header copy, eyebrow, top-level spacing, and filter control widths only. Done.
4. Keep route, permissions, query keys, data fetching, sorting, and pagination unchanged. Done.
5. Run lint, typecheck, and build. Done.
