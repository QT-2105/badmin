# Current Audit

The `/users` page already uses `AppShell`, `PageShell`, `PageHeader`, and `AuthUsersPanel`.

Current risks:

- Dense sections have uneven hierarchy.
- No approved filter/search capability exists.
- Any route/page guard changes are out of scope.
