# Feature Implementation Prompt

```text
Read AGENTS.md, docs/README.md, canonical docs, rules, relevant source, and prisma/schema.prisma when applicable.

Implement incrementally using UI -> hook -> service -> API -> repository -> Prisma, with Zustand for live runtime actions. Preserve six-item permission-filtered root navigation, contextual runtime, session-scoped players, current-state recovery, operator authority, tablet/mobile density, lightweight finance/inventory, and beta/production DB isolation.

Stop for owner approval before changing runtime lifecycle/scoring/eligibility, court lifecycle, sync strategy, completion economics, stock movement semantics, auth/permissions/bootstrap, navigation, settings storage, schema target, or production data access.

For auto-suggestion preserve immediate WAITING after match, limited late-arrival assistance, wait protection, one-shot Trận kế, End-Game, Host fallback, registered-format Couple, effective female level, same-format preference, and exact-quartet-only anti-repeat.

Remove code only after import/export graph and source search prove it unused. Run test, lint, typecheck, build, schema automation guard, and git diff check.
```
